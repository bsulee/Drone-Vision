# DXD Vision Engine

AI threat detection system for Deus X Defense.

## Phase 1: Video Input + Frame Extraction

Extract frames from video files at a configurable FPS with full metadata.

### Setup

```bash
pip install -r requirements.txt
```

### Usage

```bash
python -m dxd_vision --input video.mp4
```
